---
import { Image } from "astro:assets";
const { src, href, idx, active } = Astro.props;
import ponybill from "../assets/2025/ponybill.png";
---

<a class={`max-h-120 sm:max-h-72 md:max-h-80 lg:max-h-80 xl:max-h-104 2xl:max-h-120  items-center flex flex-col gap-y-1 lg:gap-y-2 duration-300 ${idx % 2 ? 'rotate-1 hover:rotate-3 ' : '-rotate-1 hover:-rotate-3 '} ${active ? 'grayscale-0 cursor-pointer' : 'grayscale cursor-not-allowed'} hover:scale-[1.05]`} href={active ? href : ''}>
  <div
    class="relative flex flex-col rounded border-2 border-black"
  >
  <!-- <div
    class="flex flex-col leading-snug gap-y-0.5 text-center justify-center mt-2 absolute bg-yellow-200 w-full py-2 bottom-0"
  >
    <div class="text-base text-black">Playbill</div>
  </div> -->
    <!-- <img src="/Playbill_logo.svg" /> -->
    <Image
      src={ponybill}
      width="400"
      class=""
      alt=""
    />
    <Image
      src={src}
      width="400"
      alt=""
      class=" object-cover object-top"
    />
  </div>
</a>